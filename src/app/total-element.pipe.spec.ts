import { TotalElementPipe } from './total-element.pipe';

describe('TotalElementPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalElementPipe();
    expect(pipe).toBeTruthy();
  });
});
